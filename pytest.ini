[pytest]
testpaths = tests
addopts = -ra --strict-markers
markers =
    smoke: fast sanity check that the API is reachable
    contract: cross-resource contract checks
    negative: negative / invalid-input scenarios
    schema: JSON schema validation
